# pnpm's pre-run "deps status" check re-runs install and treats unbuilt native
# optional deps (esbuild/sharp) as a hard error. We don't need those build
# scripts (vitest uses the prebuilt esbuild binary; Next bundles image handling),
# so skip the check and let scripts run directly.
verify-deps-before-run=false
